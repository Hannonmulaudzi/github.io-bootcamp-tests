function isFromCapeTown(plates){
    if( plates.startsWith('CA')){
       var numberPlates = plates.startsWith('CA');
       return true;
       
       }
       else
       {
       return false;
       }
       }
  console.log(isFromCapeTown('CA 123 908'));
  console.log(isFromCapeTown('CJ 123 908'));